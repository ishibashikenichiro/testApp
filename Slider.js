import React, {Component} from 'react';
import {AppRegistry, Text, View, StyleSheet, TouchableHighlight, TextInput, Button, ListView, FlatList, Alert, Image} from 'react-native';

export default class SliderTest extends Component {
  render() {
    return <Text>スライダー実装予定</Text>
  }
}

AppRegistry.registerComponent('SliderTest', () => SliderTest);